# Markdown to DOCX Converter

This is a simple tool to convert Markdown files to Word documents.

## How to use

1. Drop your `.md` files into the `input/` folder.
2. Run the application:
   ```bash
   python app.py
   ```
3. Find your converted `.docx` documents in the `output/` folder.
